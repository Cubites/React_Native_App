import React, { useInsertionEffect, useRef} from 'react';
import { View, Animated, Text } from 'react-native';

const Box = ({ backgroundColor, scale, setScale }) => {
  return (
    <div>Box</div>
  )
}

export default Box